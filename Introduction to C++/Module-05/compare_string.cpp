#include<bits/stdc++.h>
using namespace std;
int main(){
    string a,b;
    cin>>a>>b;
    cout<<a<<" "<<b<<endl;
    // We can compare two string using (==) sign
    if(a == b){
        cout<<"YES";
    }else{
        cout<<"NO";
    }
    return 0;
}